import { LoadIconPresenter } from '@/components/feature/load/atoms/LoadIcon';

import type { ComponentMeta, ComponentStoryObj } from '@storybook/react';

export default { component: LoadIconPresenter } satisfies ComponentMeta<
  typeof LoadIconPresenter
>;

export const Default: ComponentStoryObj<typeof LoadIconPresenter> = {};
